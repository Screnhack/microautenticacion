server:
  port: 8080
spring:
  webflux:
    base-path: /autenticacion
  application:
    name: "autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  adapters:
    r2dbc:
      host: localhost
      port: "5432"
      database: "crediya"
      schema: "autenticacion"
      username: "postgres"
      password: "12345678"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
